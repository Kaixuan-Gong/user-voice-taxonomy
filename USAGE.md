# User Voice Taxonomy Skill 使用说明

## 一、如何获取这个 Skill

### 方式一：从 GitHub 安装（推荐）

在豆包办公环境中，直接对 AI 说：

> "帮我安装 user-voice-taxonomy 这个 skill，从 GitHub 仓库 https://github.com/Kaixuan-Gong/user-voice-taxonomy 安装"

AI 会自动执行 clone 并配置到 `.user_skills/` 目录。

### 方式二：手动 Clone

```bash
cd ~/Library/Application\ Support/DoubaoWork/Default/.doubaowork/agent_mode/workspace/.user_skills/
git clone git@github.com:Kaixuan-Gong/user-voice-taxonomy.git
```

### 模型依赖

首次运行时会自动下载两个模型（约 750MB），无需手动操作：

| 模型 | 大小 | 来源 |
|---|---|---|
| BGE-small-zh-v1.5（向量模型） | ~100MB | HuggingFace `BAAI/bge-small-zh-v1.5` |
| Laya multilingual（分类模型） | ~650MB | HuggingFace `convaiinnovations/laya` |

如果网络受限，也可以手动下载后放到项目下的 `embedding_models/` 和 `laya_models/` 目录。

---

## 二、一句话介绍

**从大量用户反馈中自动生成三级标签体系并完成全量打标，最终交付一张结构化的飞书分析表格。**

---

## 三、详细能力介绍

### 核心流程

```
原始数据 → 清洗去重 → AI特征抽取 → 向量聚类 → 标签体系生成 → 分层打标 → 飞书表格交付
```

### 1. 数据准备与清洗

- **支持格式**：CSV、Excel (.xlsx)、Parquet、JSONL、飞书表格
- **自动处理**：空值过滤、噪声词去除、去重（基于文本指纹）、上下文字段提取
- **输出**：标准化记录文件、被拒绝记录明细、分批特征输入文件

### 2. AI 语义特征抽取

由执行 AI 逐条分析用户原声，提取：

- **关键问题点**：具体问题表现，而非泛泛的"体验差"
- **问题对象**：问题发生在哪个环节（配送/商品/客服等）
- **用户诉求**：用户期望的解决方式
- **原因分析**：问题产生的可能原因
- **情绪与严重度**：用户情绪倾向和问题严重程度
- **证据片段**：从原文连续截取的证据文本

### 3. 中文语义向量聚类

- 使用 BGE-small-zh-v1.5 中文向量模型将特征文本向量化
- 层次聚类自动发现问题分组
- **自动选择阈值**：扫描 0.62–0.80 多个相似度阈值，综合轮廓系数、噪声率、最大簇占比选出最优
- 输出每个簇的代表原声、高频问题点、问题对象分布

### 4. 三级标签体系生成与治理

由执行 AI 基于聚类结果生成并治理标签树：

- **三级结构**：一级（大类）→ 二级（子类）→ 三级（具体问题）
- **MECE 原则**：合并同义标签、拆分混杂标签、补齐缺失类别
- **每个标签带完整定义卡**：定义、包含范围、不包含范围、正例、反例、业务动作
- **自动校验**：检查层级合法性、ID 唯一性、同级标签相似度重叠

### 5. Laya 分层打标

- **逐层分类**：每个父节点下只比较直接子标签，避免一次面对过多候选
- **低置信拒识**：Top1 概率低于阈值（默认 0.50）或 Top1-Top2 差值过小（默认 0.10），自动进入人工复核队列
- **多意图拆分**：一条原声包含多个问题时，按 issue span 拆分分别打标
- **可追溯**：每条打标结果都带层级概率、综合置信度、标签版本、模型版本

### 6. 覆盖率迭代治理

- **过高标签**（>25%）：检查是否存在稳定子结构，满足条件后拆分
- **过低标签**（<0.5% 且样本 <20）：检查是否高风险/新兴问题，否则合并或转观察
- **未知率告警**：未知样本超过 10% 时，触发重新聚类和标签补全
- **版本管理**：标签树变更时升级版本号，保留旧标签映射

### 7. 模型微调与校准

- **金标评估**：有足够人工标注数据时，报告 Macro-F1、ECE 校准误差等指标
- **Laya 微调**：支持基于业务金标微调 Laya 决策头（冻结编码器，仅训练分类层）
- **温度校准**：在独立验证集上拟合温度参数，让输出概率可用于自动接受/拒识
- **BGE 微调**：按需对比学习微调向量模型，提升聚类纯度

### 8. 最终交付：飞书电子表格

固定 6 张子表：

| 子表 | 内容 |
|---|---|
| **运行说明** | 数据口径、模型版本、标签版本、阈值参数、处理说明 |
| **打标明细** | 每条原声的三级标签路径、概率、置信度、状态、证据片段 |
| **标签体系** | 完整标签定义卡：定义、包含、不包含、正例、反例、业务动作 |
| **标签分布** | 每个标签的数量、占比、治理建议 |
| **TOP问题** | 聚类规模、高频问题点、代表原声 |
| **人工复核队列** | 低置信、近似并列的待人工确认样本 |

### 运行模式

| 模式 | 适用场景 |
|---|---|
| `discover` | 首次分析，从零构建标签体系 |
| `label` | 已有冻结标签树，批量打标新数据 |
| `review` | 对比历史结果，检查漂移和覆盖率变化 |
| `full` | 完整跑完发现 + 打标 + 复核，直接交付飞书表格 |

### 典型使用场景

- 用户反馈/VOC 分析
- 客服工单聚类和问题分布
- App 评论/应用商店评价标签化
- 投诉数据归因和 TOP 问题挖掘
- 产品迭代的用户声音洞察
